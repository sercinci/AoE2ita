{% extends "layout.html.twig" %}

{% block title %}{{user.username}} - AoE2ita{% endblock %}
{% block meta %}

{% endblock %}
{% block content %}
    {% include 'menu.html.twig' %}
    <div>{{user.username}}</div><br>
    <img src="{{ user.avatar }}"><br>
    <a href="{{ path_for('index') }}">Home</a>
    <br>
    <p>DM: <strong>{{ user.mmr_dm }}</strong></p>
    <p>RM: <strong>{{ user.mmr_rm }}</strong></p>
    <p>Partite: <strong>{{ user.games }}</strong></p>
    {% include 'footer.html.twig' %}
{% endblock %}
{% block javascript %}
  {{ parent() }}
{% endblock %}